import React from 'react';

const Header = () => (
  <header className="fixed top-0 left-0 w-full bg-blue-600 text-white h-14 flex items-center shadow z-10">
    <div className="w-full text-center font-semibold text-base md:text-lg tracking-wide">
      ID Card Generator
    </div>
  </header>
);

export default Header;
